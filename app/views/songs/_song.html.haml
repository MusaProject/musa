%dd.small-2.large-6.columns
  = link_to song.name, "#song-#{song.id}-description"
  .content{ id: "song-#{song.id}-description" }
    %ul.inline-list.small-6.small-centered.columns
      %li
        = song.level
      %li
        = link_to 'IPA', '#', 'data-reveal-id' =>  'ipa-modal'
      %li
        = link_to 'Español', '#', 'data-reveal-id' =>  'spanish-modal'
    %p
      Description lorem ipsum

    = link_to 'Ejercicio de Vocabulario', song_path(song)

#message-modal
  = render partial: 'modal', locals: { song: song }
